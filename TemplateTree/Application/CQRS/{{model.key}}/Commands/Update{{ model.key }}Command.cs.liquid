using Application.Abstractions.Messaging;
using Application.DTO;

namespace Application.CQRS.{{ model.key }}.Commands;

public sealed record Update{{ model.key }}Command(Guid Id, {{ model.key }}DTO {{ model.key }}DTO) : ICommand;